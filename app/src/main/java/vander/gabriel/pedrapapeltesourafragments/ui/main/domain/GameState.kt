package vander.gabriel.pedrapapeltesourafragments.ui.main.domain

enum class GameState {
    NOT_INITIALIZED,
    SELECTING_PLAYERS,
    SELECTING_HAND,
    CALCULATING_MOVES,
    DISPLAYING_RESULTS,
}