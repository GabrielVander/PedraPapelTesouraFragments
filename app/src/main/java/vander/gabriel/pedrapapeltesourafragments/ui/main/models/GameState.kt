package vander.gabriel.pedrapapeltesourafragments.ui.main.models

enum class GameState {
    NOT_INITIALIZED,
    SELECTING_PLAYERS,
    SELECTING_HAND,
    CALCULATING_MOVES,
    DISPLAYING_RESULTS,
}